{
  "name": "OriumSubgraph",
  "version": "1.0.0",
  "author": "orium.network",
  "license": "MIT",
  "description": "A set of subgraphs (one for each network) for Orium Network dapps",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "graph-auth": "graph auth --product hosted-service",
    "codegen-matic": "cp subgraph-matic.yaml subgraph.yaml && graph codegen subgraph-matic.yaml",
    "build-matic": "graph build subgraph-matic.yaml",
    "deploy-matic": "graph deploy --node https://api.thegraph.com/deploy/ oriummgnt/orium-fancy-birds subgraph-matic.yaml",
    "test-matic": "cp subgraph-matic.yaml subgraph.yaml && graph test matic"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OriumNetwork/orium-subgraph.git"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.33.0",
    "gluegun": "^5.1.2"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.27.0",
    "matchstick-as": "^0.5.0"
  }
}
